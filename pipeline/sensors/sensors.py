"""Example sensors for the pipeline

Define sensors that watch for external events and start jobs.
"""

# from dagster import sensor

# @sensor
# def my_sensor(context):
#     yield RunRequest(run_key=None, run_config={})
